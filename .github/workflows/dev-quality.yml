name: 'Continuous Integration & Quality Gate'

on:
  push:
    branches: [ "dev" ]
  pull_request:
    branches: [ "dev" ]

permissions:
  contents: read

jobs:
  # 1. Job: Validate Python Code Style, Syntax and Logic
  python-quality:
    name: 'Python Style & Unit Tests'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest flake8
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run Style Check (Flake8)
        run: |
          # Stop the build if there are Python syntax errors or undefined names
          # E9, F63, F7, F82 are critical errors that prevent code from running
          flake8 ./src --count --select=E9,F63,F7,F82 --show-source --statistics
          # Optional: Exit with 0 even if there are minor style warnings (PEP8)
          flake8 ./src --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

      - name: Run Unit Tests
        run: |
          export PYTHONPATH=$PYTHONPATH:$(pwd)/src
          pytest --version
          pytest ./tests
          echo "Application logic validated successfully."

  # 2. Job: Validate Infrastructure Best Practices
  infra-linting:
    name: 'Terraform Static Analysis (TFLint)'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: infra
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup TFLint
        uses: terraform-linters/setup-tflint@v4
        with:
          tflint_version: v0.50.0

      - name: Initialize TFLint
        run: tflint --init

      - name: Run TFLint
        run: tflint -f compact